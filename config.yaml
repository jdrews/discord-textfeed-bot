# Discord Text Feed Bot Configuration
# This file configures how the bot reads from a text file and injects lines into Discord

discord:
  # Target Discord channel ID (required)
  # Replace with your actual channel ID
  channel_id: "123456789012345678"

source:
  file_path: "./books/project-gutenberg/pg844-the-importance-of-being-earnest.txt" # Path to the source text file (relative or absolute path)
  # This app was specifically tested on https://www.gutenberg.org/cache/epub/844/pg844.txt
  start_line: 78  
  end_line: 3914  # Optional - stop at this line number
  injection_mode: "character_scene"  # Options: "line", "paragraph", "character_scene"

schedule:
  # Base seconds between injections (e.g., 180 = 3 minutes)
  interval_seconds: 180
  
  # Optional: ± variance in scheduling (0 = exact timing)
  # This adds randomness to prevent predictable patterns
  randomization_seconds: 30

logging:
  # Logging level: DEBUG, INFO, WARNING, or ERROR
  level: "DEBUG"
  
  # Path to the debug log file
  file: "./logs/debug.log"
